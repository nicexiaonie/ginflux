# InfluxDB 连接配置

# InfluxDB 服务器地址
# 本地开发环境通常使用: http://localhost:8086
# 云端环境示例: https://us-west-2-1.aws.cloud2.influxdata.com
INFLUX_URL=http://localhost:8086

# InfluxDB 访问令牌
# 在 InfluxDB UI 中生成: Data -> Tokens -> Generate Token
# 需要读写权限才能完成所有测试
INFLUX_TOKEN=your-token-here

# 组织名称
# 在 InfluxDB UI 中查看: User -> About
INFLUX_ORG=your-org

# Bucket 名称
# 测试会在这个 bucket 中写入和读取数据
# 建议使用测试专用的 bucket，避免影响生产数据
INFLUX_BUCKET=test-bucket

# 注意事项:
# 1. 请确保 Token 有足够的权限 (读写 Bucket、管理 Bucket)
# 2. 部分测试会创建和删除临时 Bucket
# 3. 压力测试会写入大量数据点，请注意 Bucket 的存储配置
# 4. 请勿在生产环境运行此测试
